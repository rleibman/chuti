include "application"

# Test configuration overrides
# Provide concrete values for required fields since tests use InMemoryRepository

chuti {
  db {
    dataSource {
      user = "test-user"
      password = "test-password"
    }
  }

  session {
    secretKey = "test-secret-key-for-testing-only-at-least-128-characters-long-12345678901234567890123456789012345678901234567890123456789012345678901234567890"
  }

  oauth {
    google {
      clientId = "test-client-id.apps.googleusercontent.com"
      clientSecret = "test-client-secret"
    }
    github {
      clientId = "test-github-client-id"
      clientSecret = "test-github-client-secret"
      authorizationUri = "https://github.com/login/oauth/authorize"
      tokenUri = "https://github.com/login/oauth/access_token"
      userInfoUri = "https://api.github.com/user"
      redirectUri = "http://localhost:8079/oauth/github/callback"
      scopes = ["read:user", "user:email"]
    }
    discord {
      clientId = "test-discord-client-id"
      clientSecret = "test-discord-client-secret"
      authorizationUri = "https://discord.com/api/oauth2/authorize"
      tokenUri = "https://discord.com/api/oauth2/token"
      userInfoUri = "https://discord.com/api/users/@me"
      redirectUri = "http://localhost:8079/oauth/discord/callback"
      scopes = ["identify", "email"]
    }
  }
}
